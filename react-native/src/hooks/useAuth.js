/**
 * useAuth — re-exported from @alphinium/auth for backward compatibility.
 * New code should import directly: import { useAuth } from '@alphinium/auth';
 */
export { useAuth } from '@alphinium/auth';
