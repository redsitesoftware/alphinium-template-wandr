/**
 * AuthContext — re-exported from @alphinium/auth for backward compatibility.
 * New code should import directly: import { AuthProvider, AuthContext } from '@alphinium/auth';
 */
export { AuthProvider, AuthContext, createAuthConfig } from '@alphinium/auth';
